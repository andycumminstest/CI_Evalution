dependencies:
  override:
    - npm install:
        timeout: 240
test:
  override:
    - cd node_modules/protractor/bin
    - protractor conf.js --sauceUser $SAUCE_USERNAME --sauceKey $SAUCE_ACCESS_KEY
